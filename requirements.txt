# Versions are pinned to exactly what produced the results in results/ and
# results_report.html. Earlier versions of this file used ">=" lower bounds
# only, which meant a fresh install months later could silently pull different
# library behaviour and shift the numbers. If you only need to read the code,
# any recent version works; if you need to REPRODUCE the reported figures,
# install these exact versions.
#
# For a byte-exact environment (all 104 transitive dependencies), use
# requirements-lock.txt instead:  pip install -r requirements-lock.txt

# Core data
pandas==3.0.3
numpy==2.5.0
scikit-learn==1.9.0

# Statistics -- McNemar's test for evaluate.py significance
statsmodels==0.14.6

# Plotting for EDA
matplotlib==3.11.0
seaborn==0.13.2

# Deep learning
# torch is NOT installed from PyPI by default -- the CPU-only wheel would make
# CNN/BERT training impractically slow. Install the CUDA build first, then the
# rest of this file:
#     pip install torch==2.5.1 --index-url https://download.pytorch.org/whl/cu121
# The reported results were produced with torch 2.5.1+cu121 on CUDA 12.1.
transformers==5.12.1
datasets==5.0.0

# Text preprocessing
nltk==3.9.4

# LLM generation
openai==2.44.0
tqdm==4.68.3
python-dotenv==1.2.2

# Embeddings for CNN (GloVe download helper)
gensim==4.4.0
